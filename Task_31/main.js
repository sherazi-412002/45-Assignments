"use strict";
let userNames = ["Admin", "Ali", "Zahid", "Nouman", "Haider"];
userNames = [];
if (userNames.length === 0) {
    console.log("We need to find some users!");
}
else {
    console.log(`Hello ${userNames}!`);
}
